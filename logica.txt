
------------------------------------------------------------------------------------------------------------------
WHILE LOOP COM SWITCH CASE, SE CASO1 = A OPCAO QUE USUARIA ESCOLHER

while (opcao != 4) {
            opcao = mostrarMenu();

----------------------------------------------------------------------------------------------------------------------------------
   SE CASO1 = A OPCAO QUE USUARIA ESCOLHER =1 CONSULTA EXIBE OS Atributos E EXECUTA O METODO (consultarSaldoo(CLiente = cleiton))

            switch (opcao) {
                case 1:
                    consultarSaldo(cliente);
                    break;


    -------------------------------------------------------------------------------------------------------------------------------------
                DEFAULT DEFINE QUE SE NAO FOR NENHUM DOS CASO OU FOR 0 APARECE A MENSAGEM POR PADRAO E DA BREAK PRA VOLTAR PRO MENU INICIAL

                default:
                    JOptionPane.showMessageDialog(null, "Opção inválida! Digite um número entre 1 e 4.");
                    break;

--------------------------------------------------------------------------------------------------------------------------------------------
JOptionPane.showMessageDialog

CAIXA DE DIALAGO OU MENSSAGEBOX D0 JAVA NULL PQ ELE NAO VAI MOSTRAR NADA SO EXIBIR A MENSAGEM DENTRO DE " "
SE NULL FOSSE SALDO OU NOME ELE APARECERIA UM MSGBOX COM O nome
-------------------------------------------------------------------------------------------------------

JOptionPane.showInputDialog

EXIBE UM CAIXA TBM POREM DE INPUT OU SEJA RECEBE VAI APARECER UM CAIXA PARA DIGITAR E ENVIAR O INPUT "MENSAGEM PARA O CODIGO RESOLVER E VER QUAL CASE SE ENCAIXA"

------------------------------------------------------------------------------------------------------------------------------------------------------------------
this.() atribuir valor ao atributo o ex o tributo nome tem que ter o valor de de nome entao this.nome = nome 
